let SessionLoad = 1
let s:so_save = &g:so | let s:siso_save = &g:siso | setg so=0 siso=0 | setl so=-1 siso=-1
let v:this_session=expand("<sfile>:p")
silent only
silent tabonly
cd ~/.config/nvim
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
let s:shortmess_save = &shortmess
if &shortmess =~ 'A'
  set shortmess=aoOA
else
  set shortmess=aoO
endif
badd +9 custom_plugins/nvim-undo/lua/nvim-undo/init.lua
badd +2 lua/plugins/nvim-undo.lua
badd +1 ~/.config/nvim
badd +7 custom_plugins/nvim-undo/lua/nvim-undo/commands.lua
badd +26 custom_plugins/nvim-undo/lua/nvim-undo/core.lua
badd +22 ~/.config/nvim/custom_plugins/nvim-undo/lua/nvim-undo/time-ago.lua
badd +7 lua/plugins/essentials/lsp/servers/tsserver.lua
badd +129 ~/.config/nvim/lua/plugins/essentials/lsp/init.lua
argglobal
%argdel
$argadd ~/.config/nvim
edit ~/.config/nvim/lua/plugins/essentials/lsp/init.lua
wincmd t
let s:save_winminheight = &winminheight
let s:save_winminwidth = &winminwidth
set winminheight=0
set winheight=1
set winminwidth=0
set winwidth=1
argglobal
let s:l = 139 - ((21 * winheight(0) + 19) / 39)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 139
normal! 0
tabnext 1
if exists('s:wipebuf') && len(win_findbuf(s:wipebuf)) == 0 && getbufvar(s:wipebuf, '&buftype') isnot# 'terminal'
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=1 winwidth=20
let &shortmess = s:shortmess_save
let &winminheight = s:save_winminheight
let &winminwidth = s:save_winminwidth
let s:sx = expand("<sfile>:p:r")."x.vim"
if filereadable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &g:so = s:so_save | let &g:siso = s:siso_save
set hlsearch
nohlsearch
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
